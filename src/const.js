const EMOTIONS = ['smile', 'sleeping', 'puke', 'angry'];

const FilterType = {
  WATCHLIST: 'Watchlist',
  HISTORY: 'History',
  FAVORITES: 'Favorites',
};

const SortType = {
  DEFAULT: 'default',
  DATE: 'date',
  RATING: 'rating',
};

export {EMOTIONS, FilterType, SortType};
